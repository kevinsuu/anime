# 卡片收藏狀態同步

`useAnimeCardStatuses` 是季番表與作品目錄卡片的唯一狀態來源。它先以
`meBootstrap` 取得目前範圍的狀態，再以樂觀更新執行收藏、已看與分類操作。

卡片範圍因換頁、篩選或季別切換而變動時，尚未完成的同作品操作仍保持鎖定；
新的 bootstrap 回應不得覆蓋該作品的樂觀狀態。操作回應會以同一操作 token
驗證後才寫回，登出或重設則會使既有 token 失效。

這可避免舊快照或過期回應將「已觀看」或「已收藏」的畫面還原成舊狀態。
